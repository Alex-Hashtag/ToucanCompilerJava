package org.alex_hashtag.structures;

/**
 * Showcases:
 *  - A struct
 *  - Another struct
 *  - Some function usage
 */

public struct Person
{
    string name = nullString!(64);
    uint8 age = 0;
}

public struct Book
{
    string title = nullString!(128);
    uint16 year = 0;
    bool isAvailable = true;
}

// Possibly some function or variable
public int32 getMaxYear()
{
    return 2025;
}
