package com.example;

int32 a;
var inferredValue = 42;
const int64 max_value = 9223372036854775807;
bool f = true and false;
mutable rune unicodeChar = 'λ';
&string multilineString = """
    Multiline String
    Multiple Lines
    Wow!
""";
x += 5;
x <<= 1;
x &= 0xFF;
x++;  // Post-increment
++x;  // Pre-increment
bool result = a == b or a == (b >> 2);
bool complex = (x + 1) * 3 - 12 > 10;
&int32 pointerToInt = &a;
int32[] arr = int32[]{1, 2, 3, 4, 5};
mutable int[5] fixedSizeArray = 0;
mutable &int32[] dynamicHeapArray = int32[10];
int32[][] matrix = int32[][]{
    {1, 2, 3},
    {4, 5, 6}
};
ArrayList<int32> intList;
ArrayList<tuple<int, &string>> indexedList;
x-> x * x;
(x, y) -> x + y;
(int32 a, int32 b) -> { return a * b; };
(int32 n) -> {
    if (n == 0) return 1;
    return n * factorial(n - 1);
};
sys.call(1, "exit");
Person.new("Alex", 25);
sb.append("Hello, ").append("World!").print();